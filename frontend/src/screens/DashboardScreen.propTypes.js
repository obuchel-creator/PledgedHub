import PropTypes from 'prop-types';

// ...existing DashboardScreen code...

Dashboard.propTypes = {
  // Example prop types if you ever pass props to Dashboard
  // loading: PropTypes.bool,
  // pledges: PropTypes.array,
  // payments: PropTypes.array,
  // pledgeForm: PropTypes.object,
  // pledgeMessage: PropTypes.object,
  // creatingPledge: PropTypes.bool,
  // handlePledgeFieldChange: PropTypes.func,
  // handlePledgeSubmit: PropTypes.func,
  // resetPledgeForm: PropTypes.func,
};
